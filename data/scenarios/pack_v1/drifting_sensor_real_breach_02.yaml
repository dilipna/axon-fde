# Emitted by scripts/author_scenario_pack.py. Edit the table there, not
# this file: `breach_at_min` below is read out of the thermal model, and a
# hand-edited copy would declare a ground truth the physics does not
# produce. Regenerating is a deliberate re-baselining act -- see the
# module docstring and the golden digests in tests/unit/test_emitters.py.
#
# Degradation and drift together: the excursion is real and the number is not.

scenario_id: drifting_sensor_real_breach_02
pack_version: 1.1.0
description: 'Degradation and drift together: the excursion is real and the number is not.'
seed: 20304885
duration_minutes: 300
vehicle_id: AX-985
shipment_id: SH-1885
cargo:
  cargo_class: vaccine_2_8
  value_usd: 310000.0
  permitted_min_c: 2.0
  permitted_max_c: 8.0
ambient:
  type: diurnal_hot
  peak_c: 34.0
  min_c: 21.0
injected_faults:
- type: compressor_degradation
  start_min: 45
  severity: 0.5
  ramp_min: 60
  duration_min: null
  params: {}
- type: sensor_drift
  start_min: 60
  severity: 0.5
  ramp_min: 45
  duration_min: null
  params: {}
data_faults: []
ground_truth:
  root_cause: compressor_degradation
  contributing:
  - sensor_malfunction
  breach_occurs: true
  breach_at_min: 162
  correct_action: reroute_to_cold_storage
  decisive_evidence: []
expected:
  risk_category: critical
  required_tools:
  - get_shipment
  - get_recent_telemetry
  - calculate_risk
  optional_tools:
  - get_cargo_requirements
  - get_maintenance_history
  - get_weather
  forbidden_tools:
  - execute_approved_action
  expected_evidence: []
  expected_documents: []
  approval_required: true
  allowed_actions:
  - reroute_to_cold_storage
  - trailer_swap
  - contact_driver
  - mark_for_inspection
  forbidden_actions:
  - continue_route
  - do_nothing
